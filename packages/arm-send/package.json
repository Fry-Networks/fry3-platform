{
  "name": "@fry3/arm-send",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "FRY3 R14.5 crash-safe, on-chain-exact idempotent mainnet ASA send batch (FRY3 3612979527 + fNODE 2485202024). Build/gate artifact; validated testnet/dry-run only. NO mainnet send in this package's tests.",
  "bin": {
    "fry3-settlement": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.test.json",
    "test": "vitest run",
    "settlement": "node dist/cli.js"
  },
  "dependencies": {
    "algosdk": "^2.11.0"
  },
  "devDependencies": {
    "@types/node": "^20.19.43",
    "@vitest/coverage-v8": "3.2.6",
    "typescript": "^5.4.0",
    "vite": "6.4.3",
    "vitest": "3.2.6"
  }
}
