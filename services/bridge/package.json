{
  "name": "@fry3/bridge",
  "version": "3.0.0",
  "private": true,
  "type": "module",
  "description": "Tooth B (R3) — PG<->Mongo field-ownership bridge + drift monitor + Discord alarm",
  "scripts": {
    "test": "vitest run",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "lint": "echo lint-skip",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@prisma/client": "^7.8.0",
    "mongodb": "^6.9.0"
  },
  "devDependencies": {
    "@types/node": "^20",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
