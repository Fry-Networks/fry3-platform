{
  "name": "fry3-platform",
  "version": "3.0.0",
  "private": true,
  "description": "Fry Networks 3.0 canonical platform (clean-room rebuild)",
  "packageManager": "pnpm@9.0.0",
  "engines": { "node": ">=22" },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck"
  }
}
